#include <fcntl.h>
#include <stdio.h>
#include <string.h>
#include <sys/stat.h>
#include <unistd.h>

int main() {
  char *word = "Hello, world!\n";

  int fd = open("tmp", O_RDWR | O_CREAT, S_IRUSR | S_IWUSR);

  if (fd == -1){

  }
  
  write(fd, word, strlen(word));

  lseek(fd, -6, SEEK_CUR);
}
